<%= flash[:notice] %>
<h1>123</h1>

<%= link_to 'Add Candidate', '/candidates/new' %>

<a href="/candidates/new">Ａdd Ｃandidate</a>
<table>
  <tr>
    <td>Name</td>
    <td>Party</td>
    <td>Age</td>
    <td>Action</td>
  </tr>

  <% @candidates.each do |candidate| %>
  <tr>
    <td><a href="/candidate/<%= candidate.id %>"><%= candidate.name %></a></td>
    <td><%= candidate.party %></td>
    <td><%= candidate.age %></td>
    <td>Action</td>
  </tr>
  <% end %>
</table>